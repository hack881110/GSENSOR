#include "common.h"

#include "stdarg.h"
extern GLvoid BuildFont(GLvoid)	;							// Build Our Bitmap Font
extern GLvoid KillFont(GLvoid)	;								// Delete The Font List
extern GLvoid glPrint(const char *fmt, ...);					// Custom GL "Print" Routine
